import { ClickFuera } from './click-fuera';

describe('ClickFuera', () => {
  it('should create an instance', () => {
    const directive = new ClickFuera();
    expect(directive).toBeTruthy();
  });
});
