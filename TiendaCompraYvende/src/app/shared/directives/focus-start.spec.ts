import { FocusStart } from './focus-start';

describe('FocusStart', () => {
  it('should create an instance', () => {
    const directive = new FocusStart();
    expect(directive).toBeTruthy();
  });
});
